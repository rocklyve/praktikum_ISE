public class BaseMatrix {
    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (!(o instanceof BaseMatrix)) return false;
        // Now compare the entries
    }

    @Override
    public String toString() {...}
}
