package edu.kit.informatik;

public class BaseMatrix {
    public boolean isSameAs(BaseMatrix o) {
        if (this == o) return true;
        // Now compare the entries
        return false;
    }
    public String getRepresentation() {
        return "";
    }
}